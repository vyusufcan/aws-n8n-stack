{
  "DocumentName": "AWS-RunShellScript",
  "InstanceIds": ["i-0dfcb2dc2fb7b80f0"],
  "Parameters": {
    "commands": [
      "sudo sed -i 's/n8n.vysuufcan.cloud/n8n.vyusufcan.cloud/g' /opt/n8n/docker-compose.yml",
      "cd /opt/n8n && sudo docker compose up -d n8n",
      "sudo docker inspect n8n --format \"{{range .Config.Env}}{{println .}}{{end}}\" | grep -E \"N8N_EDITOR_BASE_URL|WEBHOOK_URL|N8N_HOST\"",
      "sudo docker logs --tail 50 n8n"
    ]
  }
}
